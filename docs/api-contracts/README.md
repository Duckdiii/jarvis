# API Contracts: Gateway ↔ Agent Runtime

Thư mục này chứa các đặc tả hợp đồng giao tiếp (WebSocket message schema, JSON schema, event types) giữa **Gateway** và **Agent Runtime**.

> **Lưu ý**: Thiết kế chi tiết hợp đồng sẽ được thực hiện tại **Phase 1** theo Roadmap.
