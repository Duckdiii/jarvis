package com.duy.jarvis.gateway.entity.enums;

/**
 * Communication channel used for interaction (VOICE or TEXT).
 * Decides whether TTS response is required.
 */
public enum ChannelType {
    VOICE,
    TEXT
}
